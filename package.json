{
  "name": "viewz",
  "version": "1.0.0",
  "description": "ViewZ headless link-opening engine",
  "private": true,
  "type": "module",
  "scripts": {
    "start:all": "concurrently \"cd backend && npm run start:all\" \"npm run dev --prefix frontend\"",
    "stop:all": "taskkill /F /IM node.exe 2>nul || true",
    "restart:all": "npm run stop:all && npm run start:all",
    "dev:backend": "cd backend && npm run start:all",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}